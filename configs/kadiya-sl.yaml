# kadiya Sri Lanka Configuration
# Auto-generated by kadiya onboard
# Provider: OpenAI
# Generated: 2026-02-15T09:43:30Z

profile:
  name: sl
  description: Sri Lanka profile - OpenAI
  version: "1.0.0"

locale:
  timezone: Asia/Colombo
  currency: LKR
  units: metric
  languages: [si, en]
  allow_singlish: true
  bilingual_output: false

tone:
  verbosity: low
  style: concise
  format: plain

agents:
  defaults:
    model: "gpt-3.5-turbo"
    max_tokens: 2048
    temperature: 0.3
    max_tool_iterations: 10
    memory_window: 30

routing:
  default_tier: cheap_general
  tiers:
    cheap_general:
      models: ["gpt-3.5-turbo"]
      max_input_tokens: 4000
      max_output_tokens: 1024
    structured:
      models: ["gpt-3.5-turbo"]
      max_input_tokens: 8000
      max_output_tokens: 2048
    fallback:
      models: ["gpt-3.5-turbo"]
      max_input_tokens: 16000
      max_output_tokens: 4096
    sensitive:
      models: ["gpt-3.5-turbo"]
      max_input_tokens: 8000
      max_output_tokens: 2048

  rules:
    - name: json_required
      condition: {needs_json: true}
      tier: structured
    - name: large_input
      condition: {input_tokens_gt: 4000}
      tier: structured
    - name: retry_escalation
      condition: {retry_count_gt: 1}
      tier: fallback
    - name: sensitive_content
      condition: {sensitivity: true}
      tier: sensitive

token_limits:
  max_input_tokens: 8000
  max_output_tokens: 2048
  intent_limits:
    translate: 1024
    summarize: 512
    format: 256
    search: 1024

conversation:
  summarize_after_turns: 5
  summarize_model: "gpt-3.5-turbo"
  retain_last_messages: 1

logging:
  log_tokens: true
  log_cost: true
  log_latency: true
  log_prompts: false
  log_pii: false

skills:
  enabled:
    - sl-translate
    - sl-telegram
    - sl-summarize
    - sl-pii-redact
    - office-excel
    - office-word
    - office-pptx
    - web-search

tools:
  restrict_to_workspace: true
  exec:
    timeout: 30
  web:
    search:
      max_results: 3
